# 1.4.0 — Fuld kodegennemgang før release + forslag til nye moduler

Baseret på en systematisk gennemgang af hele koden (sikkerhed, datarace, runtime-fejl,
død kode) lige før 1.4.0 låses. Intet er rettet endnu — dette er en plan.

## Fase 1 — Kritiske rettelser (bør laves før release)
- [x] **Spil-leaderboards kan snydes**: alle 5 spil (Tetris/BrickBreak/NexiFlyer/EndlessDodger/NeonSnake)
      skriver nu highscore via den atomare `kv:update`-operation (udvidet med valgfri `path`
      for spil med sværhedsgrad-opdelte leaderboards) i stedet for rå get/modify/set.
      Eksisterende entries uden `id` migreres automatisk ved næste indlæsning.
- [x] **`electron/main.cjs` `translation:worker-assets`**: `fs.readFileSync()`-kald er nu
      wrappet i try/catch med en tydelig fejlbesked hvis WASM/glue-filerne mangler.
- [x] **`src/lib/localKvStore.ts`**: gennemgået — `storage`-listeneren er modul-scoped
      (ét singleton-lag for hele appens levetid, ikke en per-komponent useEffect), så der
      er reelt ingen leak at rette; ingen kodeændring nødvendig.
- [x] **`UpdateNotification.tsx`**: gennemgået — `isInstalling` nulstilles allerede korrekt
      både i catch-blokken og ved `progress.phase === 'error'`; ingen faktisk bug fundet.

## Fase 2 — Bør rettes (medium)
- [x] Fjernet død kode: `src/hooks/useAppData.ts` og `src/components/GuideDialog.tsx`
      (bekræftet 0 referencer via "find all usages" før sletning).
- [x] Fjernet debug-`console.log`: `App.tsx`, `UserProfile.tsx`.
- [x] `src/lib/useAutoTranslate.ts`: tilføjet `.catch()` på `translateTextAsync()`.
- [x] `GameLeaderboardAdmin.tsx` manuel score-redigering/sletning bruger nu samme
      atomare KV-helpers som spillene selv.
- [x] `electron/updater.cjs` `readManifest()`: logger nu rigtige fejl (ugyldig JSON,
      rettigheder) men forbliver stille ved den normale "ingen opdatering endnu"-tilstand (ENOENT).
- [ ] `App.tsx` session-håndtering (`active-sessions`) bruger stadig rå read-modify-write —
      gennemgået, men IKKE rettet: data er et `Record<token,...>`-objekt (ikke et array),
      så de eksisterende array-helpers dækker det ikke uden en større generalisering.
      Lav severity og selv-korrigerende (værste fald: en "husk mig"-session går tabt,
      brugeren logger bare ind igen) — bevidst nedprioriteret for at undgå overengineering.
- [x] Krypteringsnøglen i `electron/store.cjs` er statisk og delt af alle installationer —
      noteret som kendt begrænsning (data ligger allerede bag firmaets netværksdrev).

## Fase 3 — Mindre poleringer (lav prioritet)
- [ ] Diverse `.find()`/objekt-opslag uden fuldt null-tjek ved tom/første-load data
      (MealPlan, VacationCalendar, ShiftSchedule) — kan i sjældne tilfælde vise forkert
      "ukendt bruger"/tom tilstand i stedet for at fejle helt, så lav risiko.
- [ ] `useAutoTranslate('...')`-kald til placeholders (MealPlan/ShiftSchedule) kunne
      memoizes, men er billige nok til at det ikke haster.
- [ ] BrickBreak: mange separate `setInterval`-timere til powerups — saml i én ref-liste
      der ryddes samlet, så et uventet unmount midt i spillet ikke efterlader en timer kørende.
- [ ] Partikel-id'er (`Date.now() + Math.random()`) kan kollidere i sjældne tilfælde —
      kosmetisk, ingen datatab.

## Fase 4 — Verifikation
- [ ] `npm run build` + `npm test` efter hver fase.
- [ ] Pak + deploy til `TCD-Hub 1.4.0`, kort manuel røgtest (spil, opdatering, oversættelse).
- [ ] Commit pr. fase.

---

# Forslag: Nye moduler til hubben

Ud fra hvad hubben allerede dækker (dashboard, guides, vagtplan, ferie, mails, madplan,
projekter, notesbog, spil) er der et par oplagte huller — særligt fordi appen hedder
"**Terminal Configuration & Dispatch Hub**", men reelt ikke har noget modul der
understøtter selve terminal-konfiguration/dispatch-arbejdet endnu (kun guides der
*beskriver* processen).

## Høj værdi — matcher jeres faktiske arbejde
1. **Terminal-/udstyrsregister**: hold styr på fysiske betalingsterminaler — serienummer,
   status (på lager/klargjort/afsendt/i reparation/kasseret), hvilken kunde/butik den er
   tildelt, leveringsnummer, historik. Guiderne nævner serienummer/leveringsnummer/følgeseddel
   konstant — det er tydeligvis en del af arbejdet, men der er intet sted at *registrere*
   det i appen i dag.
2. **Dispatch-/forsendelseslog**: en liste over afsendte/modtagne forsendelser med status
   (afventer/afsendt/leveret), kunde, sporingsnummer — matcher navnet "Dispatch Hub" og
   kunne kobles til terminal-registeret ovenfor (hvilken terminal fulgte med hvilken forsendelse).
3. **Fejl-/hændelseslog (tickets)**: log konkrete problemer/hændelser med terminaler eller
   processer, med status (åben/i gang/løst) og evt. link til den relevante guide som løsning.
   Giver ledelsen overblik over tilbagevendende problemer.

## Medium værdi — generelt nyttige team-hub-features
4. **Opslagstavle/nyheder**: kortere fælles opslag på forsiden (adskilt fra e-mail), med
   "set af"-markering — hurtigere end mails til "husk X i denne uge".
5. **Onboarding-tjekliste**: da signup allerede kræver manager-godkendelse, kunne en simpel
   tjekliste ("adgang oprettet", "gennemgået guide X", "udstyr udleveret") følge nye brugere.
6. **Simpelt afstemnings-/poll-modul**: hurtige ja/nej eller multiple-choice-afstemninger
   til teamet (frokost, mødetidspunkt, ferieplanlægning) — meget lav kompleksitet at bygge
   oven på KV-laget.

## Lavere prioritet / kan vente
7. **Statistik-dashboard**: nu hvor der er meget struktureret data (ferie, sygdom, spil,
   guide-reviews), kunne et lille "nøgletal"-modul til ledelsen give overblik (fx antal
   guider der snart skal revideres, sygefravær pr. måned).
8. **Forslagskasse**: anonym eller navngiven feedback til ledelsen.

**Anbefaling:** Start med Terminal-registeret (#1) hvis I vil have mest reel værdi ud af
den næste version — det er den eneste rigtige "hul" i forhold til appens formål, resten
er finpudsning af noget der allerede findes andre steder (mail, notesbog).
